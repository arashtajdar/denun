# Database Configuration
# These will be automatically provided by Railway MySQL plugin
DB_HOST=your-railway-mysql-host
DB_PORT=3306
DB_NAME=railway
DB_USER=root
DB_PASSWORD=your-railway-mysql-password

# Note: On Railway, these variables are automatically injected when you add MySQL plugin
# You don't need to set them manually
